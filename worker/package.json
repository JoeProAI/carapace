{
  "name": "@openclaw/carapace-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Carapace as a hosted Cloudflare Worker: protect agent memory writes by calling a URL.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260904.1",
    "@types/node": "^22.20.1",
    "typescript": "^5.7.0",
    "vitest": "^3.2.7",
    "wrangler": "^4.129.0"
  }
}
